// api: 'https://ivr-config.uhc.com/CCSDesktopAdminAPI/'
// ng build --prod --output-hashing=none
export const environment =
{
  production: window['production'],
  baseRoute: window['baseRoute'],
  api: window['api'],
  wizardURL: window['wizardURL']

};
